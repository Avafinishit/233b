# 引用回复功能实现说明

## 功能概述

实现了类似微信/Telegram的真正引用回复功能，而不是简单的复制粘贴。

## 实现效果

### 1. 点击"引用"后

**输入框上方显示引用预览区**：
- 浅灰背景 `rgba(0, 0, 0, 0.04)`
- 左侧彩色竖线（3px，使用渐变色）
- 显示被引用消息的作者名（角色名 或 "你"）
- 显示被引用消息的内容（单行，超长省略）
- 右侧有小叉按钮可以取消引用
- 输入框保持空白，等待用户输入

**视觉特点**：
- 轻薄设计，不抢输入框视觉
- 淡入动画（200ms）
- 毛玻璃效果

### 2. 发送消息后

**新消息上方显示引用条**：
- 浅灰背景 `rgba(0, 0, 0, 0.03)`
- 左侧细竖线（3px，半透明紫色）
- 显示被引用消息的作者名
- 显示被引用消息的内容（最多2行，超长省略）
- 可点击引用条滚动到原消息位置

**交互效果**：
- 点击引用条 → 平滑滚动到原消息
- 原消息高亮闪烁（紫色背景，800ms后消失）
- 如果原消息不存在，显示"原消息未找到"提示

### 3. 引用预览管理

**显示时机**：
- 点击长按菜单的"引用"选项后立即显示

**清除时机**：
- 点击预览区的小叉按钮
- 发送消息后自动清除

**状态保持**：
- 引用状态保存在 `currentQuotedMessage` 变量中
- 包含：消息ID、内容、作者名

## 技术实现

### 数据结构

**引用状态（currentQuotedMessage）**：
```javascript
{
    id: 'msg_xxx',           // 被引用消息的ID
    content: '消息内容',      // 被引用消息的内容
    role: 'user',            // 消息角色
    authorName: '你'         // 显示的作者名
}
```

**消息数据（chatHistory）**：
```javascript
{
    id: 'msg_xxx',
    role: 'user',
    content: '回复内容',
    timestamp: 1234567890,
    quotedMessage: {         // 引用信息（可选）
        id: 'msg_yyy',
        content: '被引用的内容',
        authorName: '角色名'
    }
}
```

### 核心函数

#### 1. quoteMessage(message)
- 保存引用信息到 `currentQuotedMessage`
- 调用 `showQuotePreview()` 显示预览
- 聚焦输入框
- 显示"已添加引用"提示

#### 2. showQuotePreview()
- 创建引用预览DOM结构
- 提取消息文本内容
- 插入到输入栏上方
- 绑定关闭按钮事件

#### 3. clearQuotePreview()
- 清空 `currentQuotedMessage`
- 移除预览DOM（带淡出动画）

#### 4. scrollToMessage(messageId)
- 查找目标消息气泡
- 平滑滚动到目标位置
- 高亮闪烁效果（800ms）

#### 5. sendUserChatContent(content, previewText)
- 构建消息数据，包含引用信息
- 调用 `createUserBubble()` 渲染（传入 quotedMessage）
- 发送后自动清除引用预览

#### 6. createMessageContentElement(content, quotedMessage)
- 如果有 quotedMessage，先渲染引用块
- 再渲染消息内容
- 引用块可点击滚动到原消息

### CSS类名

**引用预览区**：
- `.chat-quote-preview` - 预览容器
- `.chat-quote-preview-line` - 左侧竖线
- `.chat-quote-preview-content` - 内容区
- `.chat-quote-preview-author` - 作者名
- `.chat-quote-preview-text` - 消息文本
- `.chat-quote-preview-close` - 关闭按钮

**消息中的引用条**：
- `.msg-quote-block` - 引用块容器
- `.msg-quote-author` - 作者名
- `.msg-quote-text` - 消息文本（最多2行）

## 视觉设计

### 引用预览区
- **背景**：`rgba(0, 0, 0, 0.04)` + 毛玻璃
- **竖线**：3px，渐变色 `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- **作者名**：12px，紫色 `#667eea`，字重500
- **文本**：13px，深灰 `#5a5a5a`，单行省略
- **关闭按钮**：28px圆形，浅灰背景，hover变深

### 引用块
- **背景**：`rgba(0, 0, 0, 0.03)`
- **竖线**：3px，半透明紫色 `rgba(102, 126, 234, 0.6)`
- **圆角**：4px
- **内边距**：8px 12px
- **作者名**：11px，紫色，字重500
- **文本**：13px，深灰，最多2行，超长省略
- **hover效果**：背景变深 `rgba(0, 0, 0, 0.05)`

### 动画效果
- **预览淡入**：200ms，从下方10px淡入
- **预览淡出**：150ms，向下10px淡出
- **滚动**：平滑滚动，居中显示
- **高亮闪烁**：紫色背景，300ms过渡，800ms后消失

## 使用流程

### 引用AI消息
1. 长按AI消息气泡（800ms）
2. 点击"引用"选项
3. 输入框上方显示引用预览
4. 输入你的回复内容
5. 点击发送
6. 新消息上方显示引用条

### 取消引用
1. 点击引用预览区的小叉按钮
2. 引用预览消失
3. 输入框恢复正常状态

### 查看原消息
1. 点击消息中的引用条
2. 自动滚动到被引用的原消息
3. 原消息高亮闪烁

## 兼容性

### 支持的消息类型
- ✅ 文本消息
- ✅ 图片消息（显示"[图片]"）
- ✅ 表情包（显示"[表情包] 名称"）
- ✅ 语音消息（显示转文字内容或"[语音]"）

### 浏览器兼容性
- 现代浏览器（Chrome, Firefox, Safari, Edge）
- 需要支持 `backdrop-filter`（毛玻璃效果）
- 需要支持 `scrollIntoView` 平滑滚动

## 与原有功能的区别

### 旧实现（复制粘贴）
```javascript
// 旧代码：直接复制到输入框
textInput.value = `「${quoteText}」\n`;
```
- ❌ 占用输入框空间
- ❌ 无法区分引用和正文
- ❌ 无法跳转到原消息
- ❌ 不符合现代聊天应用习惯

### 新实现（真正引用）
```javascript
// 新代码：显示引用预览，保存引用关系
currentQuotedMessage = { id, content, authorName };
showQuotePreview();
```
- ✅ 输入框保持空白
- ✅ 引用和正文分离
- ✅ 可点击跳转到原消息
- ✅ 符合微信/Telegram习惯

## 数据持久化

引用信息保存在 `chatHistory` 中：
```javascript
{
    id: 'msg_123',
    role: 'user',
    content: '我的回复',
    quotedMessage: {
        id: 'msg_456',
        content: '被引用的内容',
        authorName: '角色名'
    }
}
```

- 保存到 localStorage
- 刷新页面后引用关系保持
- 可以正确渲染历史引用消息

## 测试要点

### 基础功能
- [ ] 长按AI消息 → 点击"引用" → 显示引用预览
- [ ] 引用预览显示正确的作者名和内容
- [ ] 输入框保持空白
- [ ] 点击小叉按钮可以取消引用

### 发送消息
- [ ] 输入回复内容后发送
- [ ] 新消息上方显示引用条
- [ ] 引用条显示正确的作者名和内容
- [ ] 引用预览自动消失

### 跳转功能
- [ ] 点击引用条可以滚动到原消息
- [ ] 原消息高亮闪烁
- [ ] 如果原消息被删除，显示"原消息未找到"

### 边界情况
- [ ] 引用图片消息显示"[图片]"
- [ ] 引用语音消息显示转文字或"[语音]"
- [ ] 引用超长消息正确截断
- [ ] 刷新页面后引用关系保持

### 视觉效果
- [ ] 引用预览轻薄，不抢眼
- [ ] 引用条样式克制，符合项目风格
- [ ] 动画流畅，无卡顿
- [ ] 颜色搭配和谐

## 后续优化建议

1. **引用AI消息时，AI回复也能看到引用上下文**
   - 在发送给AI的消息中包含引用信息
   - AI可以理解用户在回复哪条消息

2. **支持引用用户自己的消息**
   - 目前只能引用AI消息
   - 可以扩展到引用用户消息

3. **引用链显示**
   - 如果引用的消息本身也引用了其他消息
   - 可以显示引用链

4. **引用消息预览优化**
   - 图片消息显示缩略图
   - 语音消息显示波形图标

5. **键盘快捷键**
   - Esc 取消引用
   - Ctrl+Enter 发送带引用的消息

## 文件修改

- `css/style.css` - 新增约100行引用相关样式
- `js/app.js` - 新增约150行引用功能代码
- 修改了消息发送、渲染、气泡创建等多个函数
