# 聊天气泡长按菜单功能

## 功能概述

实现了类似微信的聊天气泡长按弹出操作菜单功能，支持复制、引用、多选、心声、撤回等操作。

## 已实现功能（第一阶段）

### 1. 长按触发机制
- **触发时长**: 800ms（从原来的450ms调整）
- **视觉反馈**: 长按时气泡轻微放大（scale 1.02）并降低亮度
- **震动反馈**: 支持设备震动（20ms）

### 2. 操作菜单样式
- **背景**: 奶白色半透明背景 + 毛玻璃效果
- **布局**: 横向排列，圆角胶囊形状
- **分隔**: 操作项之间用细线分隔
- **动画**: 淡入 + 轻微缩放动画（220ms）
- **定位**: 自动计算位置，避免超出屏幕边界

### 3. 通用操作（所有消息）

#### 📋 复制
- 复制消息文本内容
- 支持文本、语音转文字、图片/表情包标签
- 复制成功后显示toast提示

#### ☑️ 多选
- 进入多选模式
- 自动选中当前消息
- 显示多选工具栏

### 4. AI消息专属操作

#### 💭 引用
- 引用AI消息内容到输入框
- 自动截取前50字符
- 格式：「消息内容...」

#### 💫 心声
- 调用AI生成角色内心想法
- **弹窗设计**:
  - 标题"心声"
  - 加载状态：转圈动画 + "正在读取角色的内心想法..."
  - 内容区：显示生成的内心独白
  - 底部按钮：重新生成、关闭
- **AI提示词**:
  - 基于角色人设
  - 包含最近5条对话上下文
  - 要求第一人称、100-200字、展现真实内心活动
- **样式**: 奶白背景、毛玻璃、圆角20px、流畅动画

### 5. 用户消息专属操作

#### ↩️ 撤回
- 撤回自己发送的消息
- 从聊天记录中删除
- 自动更新聊天列表预览
- 显示"已撤回"提示

## 技术实现细节

### CSS类名
- `.chat-long-press-menu-backdrop` - 菜单背景遮罩
- `.chat-long-press-menu` - 菜单容器
- `.chat-long-press-menu-item` - 菜单项
- `.inner-voice-modal-backdrop` - 心声弹窗背景
- `.inner-voice-modal` - 心声弹窗容器
- `.long-press-active` - 长按激活状态

### 核心函数
- `showLongPressMenu(bubble, messageId)` - 显示长按菜单
- `closeLongPressMenu()` - 关闭菜单
- `positionLongPressMenu(menu, bubble)` - 菜单定位
- `copyMessageContent(message)` - 复制消息
- `quoteMessage(message)` - 引用消息
- `recallMessage(messageId)` - 撤回消息
- `showInnerVoice(message)` - 显示心声弹窗
- `generateInnerVoice(message, role, ...)` - 生成心声内容
- `showToast(message)` - 显示提示

### 交互逻辑
1. 长按气泡800ms触发菜单
2. 点击菜单外区域关闭菜单
3. 点击操作项执行对应功能并关闭菜单
4. 多选模式下不触发长按菜单
5. 菜单优先显示在气泡上方，空间不足时显示在下方

## 使用说明

### 触发长按菜单
1. 在聊天界面长按任意消息气泡
2. 保持按压约800ms
3. 菜单自动弹出

### 使用心声功能
1. 长按AI消息气泡
2. 点击"💫 心声"
3. 等待AI生成（约2-5秒）
4. 查看角色内心想法
5. 可点击"重新生成"获取新内容

### 引用消息
1. 长按AI消息气泡
2. 点击"💭 引用"
3. 输入框自动填充引用内容
4. 继续输入你的回复

### 撤回消息
1. 长按自己发送的消息
2. 点击"↩️ 撤回"
3. 消息立即从聊天记录中删除

## 视觉风格

- **配色**: 奶白、清透、原生感
- **圆角**: 菜单12px，弹窗20px
- **阴影**: 轻柔多层阴影
- **动画**: 流畅的淡入淡出 + 缩放
- **字体**: 系统默认字体栈
- **图标**: Emoji表情符号

## 待实现功能（后续阶段）

- [ ] 收藏消息
- [ ] 编辑角色回复
- [ ] 保存图片（图片消息）
- [ ] 查看大图（图片消息）
- [ ] 语音转文字（语音消息）
- [ ] 转发消息

## 兼容性

- 支持触摸设备和鼠标操作
- 支持震动反馈（需设备支持）
- 支持剪贴板API（需HTTPS或localhost）
- 需要现代浏览器（支持backdrop-filter）

## 注意事项

1. **长按时长**: 800ms是经过调整的最佳时长，既不会误触，也不会太慢
2. **多选模式**: 在多选模式下，长按不会触发菜单，而是直接选择/取消选择
3. **心声功能**: 需要配置有效的API密钥，否则会显示生成失败
4. **撤回限制**: 目前没有时间限制，可以撤回任意时间的消息
5. **菜单定位**: 自动避免超出屏幕，但在极小屏幕上可能需要滚动查看

## 文件修改

- `css/style.css` - 新增约300行样式代码
- `js/app.js` - 新增约400行JavaScript代码
- 修改了 `bindChatBubbleSelectionBehavior` 函数的长按逻辑

## 测试建议

1. 测试不同类型消息的长按菜单（用户消息、AI消息）
2. 测试菜单在屏幕边缘的定位
3. 测试心声功能的加载和重新生成
4. 测试引用功能的文本截取
5. 测试撤回后聊天列表的更新
6. 测试多选模式和长按菜单的互斥关系
7. 测试在不同屏幕尺寸下的表现
